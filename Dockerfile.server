# FastAPI server image. Used by docker-compose and the future
# production deploy.

FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY pyproject.toml ./
COPY src ./src

RUN uv sync --no-dev

EXPOSE 8001

ENV PYTHONUNBUFFERED=1

CMD ["uv", "run", "unison-evals-server"]
